// Entry of the stand-alone dev page. The widgets are loaded by vis-2 through module federation
// (`customWidgets.js`), never through this file, and `tasks.ts` drops the generated index.html from
// the published output.
export {};
